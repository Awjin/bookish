#
# Site Config
#

title: Bookish
    # Shown in header

baseurl:
    # Can be blank
    # Set this if e.g. you have multiple sites with the same root url

date_format: "%Y/%m/%d"

lang: "en"
    # HTML lang
    # Default: "en"

color_theme: light
    # { light }

contact:
    # Can be blank
    name:  Awjin Ahn
    email: awjinahn@gmail.com
    github_username:   Awjin
    linkedin_username: awjin
    medium_username:   awjypodgy
    twitter_username:  awjypodgy


#
# Homepage Config
#

listing_limit: 2
    # { blank = no limit }
    # Number of posts shown on the homepage
    # Remaining posts are listed in Archive
    # Featured posts do not count against limit

show_dates: true
    # { true / false }
    # Show the published date next to the title

show_blurbs: true
    # { true / false }
    # Show the post blurb under the title
    # Blurb can be added in a post's front matter

show_featured: true
    # { true / false }
    # Show featured posts pinned to the top

description: A crisp, readable Jekyll theme.
    # Can be blank
    # Included in head and shown footer

copyright: MIT License
    # Can be blank
    # Shown in footer


#
# Post Config
#

show_recent_posts: true
    # { true / false }
    # Show recent posts at the end of each post

recent_listing_limit: 3
    # Number of recent posts to display


#
# Build Config
#

markdown: kramdown
    # Markdown engine
    # Default: kramdown

exclude:
    - Gemfile
    - Gemfile.lock
